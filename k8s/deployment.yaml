apiVersion: apps/v1
kind: Deployment
metadata:
  name: react-nginx-deployment
spec:
  replicas: 3
  selector:
    matchLabels:
      app: reactTest
  template:
    metadata:
      labels:
        app: reactTest
    spec:
      containers:
        - name: reactTest
          image: gcr.io/yapp16-findzone/react-test:latest
          ports:
            - containerPort: 80